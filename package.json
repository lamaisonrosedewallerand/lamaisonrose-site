{
  "name": "lamaisonrose-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-content.mjs && node scripts/build-frontend.mjs",
    "dev": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "esbuild": "^0.28.0"
  }
}
